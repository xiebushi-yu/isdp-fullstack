<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 不输出logback初始化日志 -->
    <statusListener class="ch.qos.logback.core.status.NopStatusListener" />
    <!-- 日志输出格式- 控制台 -->
    <property name="log.pattern.console" value="%d{HH:mm:ss.SSS} [%thread] %-5level %logger{20} - [%method,%line] - %msg%n" />
    <!-- 日志输出格式 - 生产环境不输出method -->
    <property name="log.pattern.file" value="%d{HH:mm:ss.SSS} [%thread] %-5level %logger{20}  - %msg%n" />

    <!-- 控制台输出 -->
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${log.pattern.console}</pattern>
        </encoder>
    </appender>

    <!-- 文件输出 -->
    <appender name="file" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/isdp.log</file>
        <encoder>
            <pattern>${log.pattern.file}</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/isdp.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- 开发环境：控制台输出 -->
    <springProfile name="dev">
        <root level="info">
            <appender-ref ref="console" />
        </root>
        <logger name="org.springframework" level="info" />
        <logger name="edu.scau.mis" level="debug" />
    </springProfile>

    <!-- 生产环境：文件输出 -->
    <springProfile name="prod">
        <root level="warn">
            <appender-ref ref="file" />
        </root>
        <logger name="org.springframework" level="warn" />
        <logger name="edu.scau.mis" level="warn" />
    </springProfile>

</configuration>